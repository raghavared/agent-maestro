{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agents-ui.maestro.dev/schemas/session-event.json",
  "title": "SessionEvent",
  "description": "An event that occurred during a session's execution, capturing actions, outputs, or state changes",
  "type": "object",
  "required": ["id", "timestamp", "type"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Unique identifier for this session event",
      "examples": ["evt_abc123", "event_001"]
    },
    "timestamp": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix timestamp (milliseconds) when this event occurred",
      "examples": [1704067200000]
    },
    "type": {
      "type": "string",
      "minLength": 1,
      "description": "Event type identifier (e.g., command, output, error, tool_call)",
      "examples": ["command_executed", "stdout", "tool_call", "error"]
    },
    "data": {
      "description": "Event-specific data payload, structure varies by event type",
      "examples": [
        {"command": "npm install", "exitCode": 0},
        {"output": "Installation complete"},
        {"error": "Connection timeout"}
      ]
    }
  },
  "additionalProperties": false
}
