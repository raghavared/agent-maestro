<maestro_prompt role="worker" strategy="tree" version="1.0">
  <task>
    <id>${TASK_ID}</id>
    <title>${TASK_TITLE}</title>
    <description>${TASK_DESCRIPTION}</description>
    <priority>${TASK_PRIORITY}</priority>
    <acceptance_criteria>${ACCEPTANCE_CRITERIA}</acceptance_criteria>
  </task>

  <task_tree>${ALL_TASKS}</task_tree>

  <context>
    <codebase>${CODEBASE_CONTEXT}</codebase>
    <related_tasks>${RELATED_TASKS}</related_tasks>
    <project_standards>${PROJECT_STANDARDS}</project_standards>
  </context>

  <tree_workflow>
    <step>Review full tree and dependency flow.</step>
    <step>Choose an execution order based on dependencies.</step>
    <step>Report progress per subtask.</step>
    <step>Report blockers and continue unblocked branches.</step>
    <step>Complete session only when all subtasks are done.</step>
  </tree_workflow>

  <progress_format>maestro session report progress "SUBTASK COMPLETE [task-id]: summary"</progress_format>
</maestro_prompt>
