# Task list styles reference

The task list UI uses shared styling in `maestro-ui/src/styles.css`.

Highlights for task list integration:
- `taskListsPanel`, `taskListItem`, `taskListTaskRow`, and modal classes for list creation/add tasks
- Terminal-themed empty/loading/error state styles reused from task/tabs

Full stylesheet is large; attaching this summary instead of the full file.
