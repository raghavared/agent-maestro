# Event Flow Documentation

## Overview

This document maps **every user action** to **server API calls** to **WebSocket events** to **UI updates**.

---

## Event Flow Diagrams

### Legend

```
[User Action]  →  API Call  →  Server State  ⟳  WebSocket Event  →  UI Update
```

---

## 1. Create Task

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks "$ new task" in MaestroPanel          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI: CreateTaskModal opens                                       │
│     User fills: title, description, priority                    │
│     User clicks "Create"                                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ API CALL: POST /api/tasks                                       │
│ {                                                               │
│   projectId: "proj_123",                                        │
│   title: "Implement login",                                     │
│   description: "...",                                           │
│   priority: "high"                                              │
│ }                                                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   1. Creates Task object                                        │
│   2. Assigns ID: task_abc123                                    │
│   3. Sets status: "pending"                                     │
│   4. Stores in tasks map                                        │
│   5. Broadcasts WebSocket event                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ WEBSOCKET EVENT: task:created                                   │
│ {                                                               │
│   event: "task:created",                                        │
│   data: {                                                       │
│     id: "task_abc123",                                          │
│     projectId: "proj_123",                                      │
│     title: "Implement login",                                   │
│     status: "pending",                                          │
│     priority: "high",                                           │
│     sessionIds: [],                                             │
│     sessionCount: 0,                                            │
│     createdAt: 1234567890,                                      │
│     updatedAt: 1234567890                                       │
│   }                                                             │
│ }                                                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATE:                                                      │
│                                                                 │
│ MaestroPanel:                                                   │
│   - onTaskCreated callback fires                                │
│   - Checks if task.projectId === currentProjectId              │
│   - If yes: setTasks([newTask, ...tasks])                      │
│   - Task appears at top of list                                 │
│                                                                 │
│ SessionsSection: (no update - task has no sessions yet)         │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Call | WebSocket Event | Updates |
|---------|----------|-----------------|---------|
| Click "$ new task" | `POST /api/tasks` | `task:created` | MaestroPanel adds task to list |

---

## 2. Work on Task (Create Session)

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks "$ exec" on a task                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI: MaestroPanel.handleWorkOnTask(task)                         │
│     - Calls App.createMaestroSession()                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ App.createMaestroSession():                                     │
│   1. Generates maestroSessionId                                 │
│   2. Calls maestroClient.createSession()                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ API CALL: POST /api/sessions                                    │
│ {                                                               │
│   id: "sess_task_abc123_1234567890",                            │
│   projectId: "proj_123",                                        │
│   taskIds: ["task_abc123"],                                     │
│   name: "Implement login"                                       │
│ }                                                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   1. Creates Session object                                     │
│   2. Links session ↔ task bidirectionally:                      │
│      - session.taskIds = ["task_abc123"]                        │
│      - task.sessionIds.push("sess_...")                         │
│      - task.sessionCount++                                      │
│   3. Stores in sessions map                                     │
│   4. Updates task in tasks map                                  │
│   5. Broadcasts multiple WebSocket events (see below)           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ WEBSOCKET EVENTS (in order):                                    │
│                                                                 │
│ 1. session:created                                              │
│    data: { id, projectId, taskIds, name, ... }                 │
│                                                                 │
│ 2. task:updated                                                 │
│    data: { id, ..., sessionCount: 1, sessionIds: [...] }       │
│                                                                 │
│ 3. session:task_added                                           │
│    data: { sessionId: "sess_...", taskId: "task_abc123" }      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ App.createMaestroSession() continues:                           │
│   3. Spawns terminal via invoke('create_session')               │
│   4. Creates UI session object with maestroSessionId            │
│   5. Adds to sessions state                                     │
│   6. Sets as active session                                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATES:                                                     │
│                                                                 │
│ SessionsSection (Left Panel):                                   │
│   - New terminal appears with maestroSessionId field            │
│   - Shows 🎯 Maestro indicator                                  │
│   - If expanded: fetches tasks, shows task list                 │
│                                                                 │
│ MaestroPanel (Right Panel):                                     │
│   - onTaskUpdated: Updates task in list                         │
│   - Task now shows sessionCount: 1                              │
│                                                                 │
│ TaskListItem:                                                   │
│   - If task is expanded and sessions visible:                   │
│     onTaskSessionAdded → fetches session, adds to cache         │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Call | WebSocket Events | Updates |
|---------|----------|------------------|---------|
| Click "$ exec" | `POST /api/sessions` | `session:created`<br>`task:updated`<br>`session:task_added` | SessionsSection adds terminal<br>MaestroPanel updates task count<br>TaskListItem adds session to cache |

---

## 3. Close Terminal (Delete Session)

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks × on terminal session                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ App.onCloseSession(sessionId):                                  │
│   1. Finds session in state                                     │
│   2. If session.maestroSessionId exists:                        │
│      → completeMaestroSession(maestroSessionId)                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ completeMaestroSession():                                       │
│   1. PATCH /api/sessions/:id { status: 'completed' }           │
│   2. DELETE /api/sessions/:id                                   │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   1. Marks session as completed                                 │
│   2. Deletes session from map                                   │
│   3. Updates all linked tasks:                                  │
│      - Removes sessionId from task.sessionIds                   │
│      - Decrements task.sessionCount                             │
│   4. Broadcasts WebSocket events                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ WEBSOCKET EVENTS:                                               │
│                                                                 │
│ 1. session:deleted                                              │
│    data: { id: "sess_..." }                                    │
│                                                                 │
│ 2. task:updated (for each linked task)                          │
│    data: { id, ..., sessionCount: 0, sessionIds: [] }          │
│                                                                 │
│ 3. session:task_removed (for each linked task)                  │
│    data: { sessionId: "sess_...", taskId: "task_..." }         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATES:                                                     │
│                                                                 │
│ SessionsSection:                                                │
│   - Terminal session removed from list                          │
│                                                                 │
│ MaestroPanel:                                                   │
│   - onTaskUpdated: Updates task sessionCount to 0              │
│   - Task shows no active sessions                               │
│                                                                 │
│ TaskListItem:                                                   │
│   - onSessionDeleted: Removes session from cache                │
│   - If sessions expanded: session disappears from list          │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Calls | WebSocket Events | Updates |
|---------|-----------|------------------|---------|
| Click × on terminal | `PATCH /api/sessions/:id`<br>`DELETE /api/sessions/:id` | `session:deleted`<br>`task:updated`<br>`session:task_removed` | SessionsSection removes terminal<br>MaestroPanel updates task count<br>TaskListItem removes from cache |

---

## 4. Expand Session to View Tasks

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks ▸ on session with maestroSessionId     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ SessionsSection.toggleSession(sessionId, maestroSessionId):     │
│   - Sets expandedSessions.add(sessionId)                        │
│   - Triggers fetchMaestroData(sessionId, maestroSessionId)      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ fetchMaestroData():                                             │
│   1. GET /api/sessions/:maestroSessionId                        │
│   2. For each taskId in session.taskIds:                        │
│      GET /api/tasks/:taskId                                     │
│   3. Stores in sessionTasks cache                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   - Returns Session object                                      │
│   - Returns Task objects                                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATE:                                                      │
│                                                                 │
│ SessionsSection:                                                │
│   - Session expands to show:                                    │
│     • Working on N tasks:                                       │
│     • [RUN] Implement login [HIGH]                              │
│     • [IDLE] Add auth middleware [MEDIUM]                       │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Calls | WebSocket Events | Updates |
|---------|-----------|------------------|---------|
| Click ▸ to expand session | `GET /api/sessions/:id`<br>`GET /api/tasks/:id` (for each) | None (read-only) | SessionsSection renders task list |

---

## 5. Expand Task to View Sessions

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks ▶ on task row                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ TaskListItem:                                                   │
│   - Sets isExpanded = true                                      │
│   - Renders subsections (Sessions, Subtasks)                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks ▸ Active Sessions (N)                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ TaskListItem:                                                   │
│   - Sets showSessions = true                                    │
│   - useEffect triggers fetchSessions()                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ fetchSessions():                                                │
│   GET /api/sessions?taskId=task_abc123                          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   - Filters sessions where taskIds includes task_abc123         │
│   - Returns Session[]                                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATE:                                                      │
│                                                                 │
│ TaskListItem:                                                   │
│   - Stores sessions in taskSessions cache                       │
│   - Renders session list:                                       │
│     ┌──────────────────────┐                                    │
│     │ session-1      ↗  × │                                    │
│     └──────────────────────┘                                    │
│     ┌──────────────────────┐                                    │
│     │ session-2      ↗  × │                                    │
│     └──────────────────────┘                                    │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Calls | WebSocket Events | Updates |
|---------|-----------|------------------|---------|
| Click ▸ Active Sessions | `GET /api/sessions?taskId=...` | None (read-only) | TaskListItem renders session list |

---

## 6. Remove Task from Session 

##COMMENT -> Does this make sense -> can we remove task from session, because we will be prompting claude about the task when the session starts

##so we cannot remove task from the session for now, later we will try to implement this, not needed for now

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks × next to session in task's session    │
│              list                                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ TaskListItem.handleRemoveSession(sessionId):                    │
│   1. Optimistically removes session from UI cache               │
│   2. Calls onRemoveTaskFromSession(sessionId, taskId)           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MaestroPanel.handleRemoveTaskFromSession():                     │
│   DELETE /api/sessions/:sessionId/tasks/:taskId                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   1. Removes taskId from session.taskIds                        │
│   2. Removes sessionId from task.sessionIds                     │
│   3. Decrements task.sessionCount                               │
│   4. Broadcasts WebSocket events                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ WEBSOCKET EVENTS:                                               │
│                                                                 │
│ 1. session:updated                                              │
│    data: { id, taskIds: [...], ... }                           │
│                                                                 │
│ 2. task:updated                                                 │
│    data: { id, sessionCount: N-1, sessionIds: [...] }          │
│                                                                 │
│ 3. session:task_removed                                         │
│    data: { sessionId, taskId }                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATES:                                                     │
│                                                                 │
│ TaskListItem:                                                   │
│   - Session already removed (optimistic update)                 │
│   - onTaskSessionRemoved: Confirms removal                      │
│                                                                 │
│ MaestroPanel:                                                   │
│   - onTaskUpdated: Updates task sessionCount                    │
│                                                                 │
│ SessionsSection:                                                │
│   - onSessionTaskRemoved: Removes task from session cache       │
│   - If session expanded: task disappears from list              │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Call | WebSocket Events | Updates |
|---------|----------|------------------|---------|
| Click × to remove task from session | `DELETE /api/sessions/:sid/tasks/:tid` | `session:updated`<br>`task:updated`<br>`session:task_removed` | TaskListItem confirms removal<br>MaestroPanel updates count<br>SessionsSection updates cache |

---

## 7. Update Task Status

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User updates task in TaskDetailModal               │
│              (e.g., marks as completed)                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MaestroPanel.handleUpdateTask(taskId, { status: 'completed' }): │
│   PATCH /api/tasks/:taskId                                      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   1. Updates task object                                        │
│   2. Sets completedAt timestamp                                 │
│   3. Broadcasts WebSocket event                                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ WEBSOCKET EVENT: task:updated                                   │
│ data: { id, status: 'completed', completedAt: ... }            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATES:                                                     │
│                                                                 │
│ MaestroPanel:                                                   │
│   - onTaskUpdated: Replaces task in list                        │
│   - Task row updates: [IDLE] → [OK] ✓                          │
│   - Completed count increments                                  │
│                                                                 │
│ SessionsSection:                                                │
│   - If session expanded and showing this task:                  │
│     onTaskUpdated: Updates task in sessionTasks cache           │
│     Status badge updates                                        │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Call | WebSocket Event | Updates |
|---------|----------|-----------------|---------|
| Update task in modal | `PATCH /api/tasks/:id` | `task:updated` | MaestroPanel updates task in list<br>SessionsSection updates task in cache |

---

## 8. Delete Task

### Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: User clicks "> rm -rf" in task menu                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MaestroPanel.handleDeleteTask(taskId):                          │
│   DELETE /api/tasks/:taskId                                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ MAESTRO SERVER:                                                 │
│   1. Removes task from tasks map                                │
│   2. Removes taskId from all linked sessions                    │
│   3. Broadcasts WebSocket events                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ WEBSOCKET EVENTS:                                               │
│                                                                 │
│ 1. task:deleted                                                 │
│    data: { id: "task_abc123" }                                 │
│                                                                 │
│ 2. session:updated (for each linked session)                    │
│    data: { id, taskIds: [...] }                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ UI UPDATES:                                                     │
│                                                                 │
│ MaestroPanel:                                                   │
│   - onTaskDeleted: Removes task from list                       │
│   - Task disappears from UI                                     │
│                                                                 │
│ SessionsSection:                                                │
│   - onTaskDeleted: Removes task from all session caches         │
│   - If session expanded: task disappears from task list         │
└─────────────────────────────────────────────────────────────────┘
```

### Triggering Events

| Trigger | API Call | WebSocket Events | Updates |
|---------|----------|------------------|---------|
| Click "> rm -rf" | `DELETE /api/tasks/:id` | `task:deleted`<br>`session:updated` (for each) | MaestroPanel removes from list<br>SessionsSection removes from caches |

---

## Summary Table: All Events

| User Action | API Call | WebSocket Events | Components Updated |
|-------------|----------|------------------|-------------------|
| **Create task** | `POST /api/tasks` | `task:created` | MaestroPanel |
| **Work on task** | `POST /api/sessions` | `session:created`<br>`task:updated`<br>`session:task_added` | App (session)<br>MaestroPanel (task)<br>TaskListItem (cache) |
| **Close terminal** | `PATCH /api/sessions/:id`<br>`DELETE /api/sessions/:id` | `session:deleted`<br>`task:updated`<br>`session:task_removed` | App (session)<br>MaestroPanel (task)<br>TaskListItem (cache) |
| **Expand session** | `GET /api/sessions/:id`<br>`GET /api/tasks/:id` | None | SessionsSection (cache) |
| **Expand task** | `GET /api/sessions?taskId=...` | None | TaskListItem (cache) |
| **Remove task from session** | `DELETE /api/sessions/:sid/tasks/:tid` | `session:updated`<br>`task:updated`<br>`session:task_removed` | All panels (update caches) |
| **Update task** | `PATCH /api/tasks/:id` | `task:updated` | MaestroPanel<br>SessionsSection (cache) |
| **Delete task** | `DELETE /api/tasks/:id` | `task:deleted`<br>`session:updated` | MaestroPanel<br>SessionsSection (cache) |

---

## Next: State Synchronization Patterns

See **[03-STATE-SYNC.md](./03-STATE-SYNC.md)** for detailed state synchronization strategies.
